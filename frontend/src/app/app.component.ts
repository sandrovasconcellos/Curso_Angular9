import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl:'app.component.html',
})
export class AppComponent {
  // nome = 'Variavel do componente.ts sendo apresentada no componente.html, atraves da interpolação {{}}';
}
